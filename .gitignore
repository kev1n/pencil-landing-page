# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.gstack/
.vercel

# Chrome-store rendering output. Keep the HTML sources + build script;
# ignore the rasterized binaries — they regenerate from `build.sh` on
# demand, and the canonical copy of the marquee lives in the pencil.nu
# repo's README.
chrome-store-assets/out/
.env
